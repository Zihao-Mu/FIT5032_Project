@model IEnumerable<FIT5032_Project.Models.OrderMenu>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Menu.DishId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Order.OrderId)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Menu.DishId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Order.OrderId)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.OrderMenuId }) |
                @Html.ActionLink("Details", "Details", new { id = item.OrderMenuId }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.OrderMenuId })
            </td>
        </tr>
    }

</table>
